# SOS — Next.js env (set in Vercel). anon key = client-safe.
# service_role must NEVER be here; it is injected only into Edge Functions.
NEXT_PUBLIC_SUPABASE_URL=https://cxdqkjvtpilvouwtbgdy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
