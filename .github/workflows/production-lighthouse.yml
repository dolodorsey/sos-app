name: S.O.S. Production Lighthouse

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "41 9 * * *"

permissions:
  contents: read

concurrency:
  group: lighthouse-${{ github.repository }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Audit production customer surfaces
        uses: treosh/lighthouse-ci-action@12.6.2
        with:
          urls: |
            https://thesuperherosonstandby.com/
            https://thesuperherosonstandby.com/login
            https://thesuperherosonstandby.com/provider
            https://thesuperherosonstandby.com/become-a-provider
          configPath: "./lighthouserc.json"
          uploadArtifacts: true
          temporaryPublicStorage: false
